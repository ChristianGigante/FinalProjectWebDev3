let retrieveOne = (model,req,res) => {
// model - from server to database
// req - from client to server
// res - from server to client
}
let retrieveAll = (model,req,res) => {

};

module.exports = { retrieveOne, retrieveAll }
